int main()
{   char str[]="STRING";
    int i;
    for(i=0;str[i]!='\0';i++)
        {
            printf("character=%c\t",str[i]);
            printf("character=%p\n",&str[i]);
        }
    return 0;
}
